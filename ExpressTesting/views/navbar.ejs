<nav>
    <ul>
        <% for (let i = 0; i < links.length; i++) { %>
            <li>
                <a href = "<%= links[i].href %>/<%= user %>">
                    <span><%= links[i].text %></span>
                </a>
            </li>
            <% } %>
    </ul>
</nav>

